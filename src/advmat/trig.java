package advmat;
import java.lang.Math;
public class trig {
		public static void calc(double x) {
			//double y=Math.sin(Math.toDegrees(x))+Math.cos(Math.toDegrees(x))+Math.tan(Math.toDegrees(x));
			double y=Math.sin((x));
			System.out.println("y="+y);
		}
}
